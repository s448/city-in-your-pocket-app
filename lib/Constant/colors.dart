import 'package:flutter/material.dart';

class ColorManager {
  // Primary color shades of green
  static const primaryColor = Color(0xFF235ACE);
  static const primaryColorDark = Color(0xFF062273);
  static const primaryColorLight = Color(0xFF7AB0EE);

  // Secondary color
  static const secondaryColor = Colors.white;

  // Accent color
  static const accentColor = Color.fromARGB(255, 230, 240, 253);

  //unselected items
  static const navBarUnselectedColor = Colors.grey;
}
