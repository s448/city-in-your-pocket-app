import 'dart:ui';

import 'package:flutter/material.dart';

class ColorManager {
  // Primary color shades of green
  static const primaryColor = Color(0xFF00A878);
  static const primaryColorDark = Color(0xFF00785E);
  static const primaryColorLight = Color(0xFF57E8AE);

  // Secondary color
  static const secondaryColor = Colors.white;

  // Accent color
  static const accentColor = Colors.orange;

  //unselected items
  static const navBarUnselectedColor = Colors.grey;
}